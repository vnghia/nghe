[workspace]
git_only = true
git_tag_name = "v{{ version }}"
git_tag_enable = true
git_release_enable = true

pr_branch_prefix = "release-plz-"
pr_labels = ["release"]

publish = false
release_always = false
semver_check = false
